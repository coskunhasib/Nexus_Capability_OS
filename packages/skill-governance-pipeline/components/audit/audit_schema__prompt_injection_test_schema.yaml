id: prompt_injection_test_schema
name: Prompt Injection Test Evidence Schema
type: evidence_schema
status: canonical
purpose: Decision-proof record for the prompt-injection adversarial test (PROMPT_INJECTION_TEST_REPORT).
owner_domain: sdlc
allowed_usage:
- Record prompt-injection / tool-misuse / poisoning defect-sweep evidence.
forbidden_usage:
- Used to record process history (trace_schema).
governance_profiles:
- evidence_required
audit_schemas:
- agentic_eval_evidence_schema
- stage_gate_trace_schema
notes: Stage 18 evidence schema (companion to agentic_eval_evidence_schema).
applies_to:
- sdlc_stage:ai_agent_safety_evaluation
required_fields:
- evidence_id
- type
- produced_by
- related_stage
- source_artifacts
- summary
- status
- created_at
producer: ai_safety_agent
consumer: evidence_graph_agent
retention: retain_with_release_evidence
redaction_rules:
- no_raw_secret_exposure
- redact_injection_payload_secrets
used_by:
  pipelines:
  - sdlc_pipeline
  stages:
  - ai_agent_safety_evaluation
  agents:
  - ai_safety_agent
