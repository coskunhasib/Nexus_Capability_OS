id: skill_misuse_warning_schema
name: Skill Misuse Warning Schema
type: evidence_schema
status: candidate
purpose: Decision-proof record for generated skill misuse warnings (SKILL_MISUSE_WARNINGS).
owner_domain: skill_governance
allowed_usage:
- Record skill misuse-warning decision-proof evidence.
forbidden_usage:
- Used to record process history (that is skill_governance_trace_schema).
governance_profiles:
- evidence_required_policy
- misuse_warning_required_gate
- locked_semantics_enforcement_policy
audit_schemas:
- skill_governance_evidence_schema
- skill_governance_trace_schema
notes: NEW (skill governance). Stage misuse_warning_generation; owner skill_runtime_safety_agent.
  Enforces locked semantics (e.g. prism brainstorming-only, verification-loop bug-finding-only).
applies_to:
- skill_governance_pipeline
- skill_governance_stage:misuse_warning_generation
required_fields:
- evidence_id
- type
- produced_by
- related_stage
- skill_id
- misuse_warnings
- status
- created_at
producer: skill_runtime_safety_agent
consumer: skill_librarian_agent
retention: retain_with_skill_registry_entry
redaction_rules:
- no_raw_secret_exposure
used_by:
  pipelines:
  - skill_governance_pipeline
  stages:
  - misuse_warning_generation
  agents:
  - skill_runtime_safety_agent
