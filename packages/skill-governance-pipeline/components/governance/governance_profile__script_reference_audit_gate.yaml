id: script_reference_audit_gate
name: Script / Reference Audit Gate
type: stage_gate_profile
status: candidate
purpose: 'Skill Governance gate: an unsafe skill script may not pass un-mitigated;
  static safety/ side-effect audit over scripts/** and references/** (skill-governance-pipeline.md
  §2.3).'
owner_domain: skill_governance
allowed_usage:
- Gate that unsafe skill scripts are mitigated before binding/activation.
forbidden_usage:
- Used outside the Skill Governance Pipeline.
governance_profiles:
- skill_governance_pipeline_governance
- evidence_required
audit_schemas:
- script_audit_evidence_schema
- skill_governance_trace_schema
scope: stage
applies_to:
- script_reference_audit
severity_default: blocker
used_by:
- script_reference_audit
aliases:
- skill_safety_gate
notes: NEW. In pipeline-catalog-review.md §4.1 the consolidated header list names
  the five gates explicitly requested; this gate is part of the same Skill Governance
  set (review §4.1 lists it). verification-loop used here strictly as defect/bug-finding
  (locked semantics). The pre-R9 catalog-YAML definition_contract variant `skill_safety_gate`
  is retained under aliases only as the historical reconciliation record; after R9
  collapsed pipeline_catalog.yaml into a summary-only catalog, the authoritative per-pipeline
  source is the markdown definition_doc.
