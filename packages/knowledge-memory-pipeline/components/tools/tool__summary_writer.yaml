id: summary_writer
name: Summary Writer
type: tool
category: tool
status: candidate
purpose: Produce a faithful, source-grounded MEMORY_SUMMARY of the classified/redacted
  memory item.
owner_domain: knowledge_management
provided_by_core_ability: null
input_schema:
  classified_item: object
output_schema:
  memory_summary_ref: string
side_effects: writes_artifact_store
risk_level: medium
requires_sandbox: true
governance_profiles:
- tool_call_governance
- sandbox_execution_governance
audit_schemas:
- tool_call_trace_schema
- evidence_item_schema
allowed_callers:
- memory_summarization_agent
allowed_usage:
- Produce the grounded MEMORY_SUMMARY (summary_generation).
forbidden_usage:
- Adding unsupported content not grounded in the classified source.
used_by:
  pipelines:
  - knowledge_memory_pipeline
  pipeline_stages:
  - knowledge_memory_pipeline.summary_generation
notes: NEW (knowledge/memory) per review §4.3. Uses summarization_model; calibrated
  by the calibrate skill.
