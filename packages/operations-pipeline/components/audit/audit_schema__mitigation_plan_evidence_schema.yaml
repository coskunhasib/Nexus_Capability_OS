id: mitigation_plan_evidence_schema
name: Mitigation Plan Evidence Schema
type: evidence_schema
status: candidate
purpose: Decision-proof record for the incident mitigation plan.
owner_domain: operations_reliability
allowed_usage:
- Record incident mitigation-plan decision-proof evidence.
forbidden_usage:
- Used to record process history (trace_schema).
governance_profiles:
- evidence_required_policy
- mitigation_plan_required_gate
audit_schemas:
- rollback_fix_evidence_schema
- stage_gate_trace_schema
notes: NEW (operations). Stage mitigation_plan.
applies_to:
- operations_pipeline
- operations_stage:mitigation_plan
required_fields:
- evidence_id
- type
- produced_by
- related_stage
- incident_id
- mitigation_steps
- source_artifacts
- status
- created_at
producer: mitigation_planning_agent
consumer: incident_response_agent
retention: retain_with_postmortem
redaction_rules:
- no_raw_secret_exposure
used_by:
  pipelines:
  - operations_pipeline
  stages:
  - mitigation_plan
  agents:
  - mitigation_planning_agent
