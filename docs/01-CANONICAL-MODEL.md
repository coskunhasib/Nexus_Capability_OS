# Canonical Model

Nexus Capability OS uses a registry + graph model as the source of truth.

The current UI tree is a visual projection. The canonical product model is stored in `registry/` and described in `docs/`.
